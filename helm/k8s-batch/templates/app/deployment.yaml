apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "k8s-batch.fullname" . }}
  labels:
    {{- include "k8s-batch.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
spec:
  {{- if not .Values.hpa.enabled }}
  # Omit replicas when HPA is enabled to prevent Helm/HPA conflicts
  replicas: {{ .Values.app.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "k8s-batch.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: app
  template:
    metadata:
      labels:
        {{- include "k8s-batch.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: app
      annotations:
        # Triggers rolling restart when ConfigMap content changes
        checksum/config: {{ include (print .Template.BasePath "/app/configmap.yaml") . | sha256sum }}
        {{- with .Values.app.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "k8s-batch.fullname" . }}
      {{- with .Values.app.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.app.podSecurityContext | nindent 8 }}
      initContainers:
        - name: wait-for-mysql
          image: "{{ .Values.global.initImage.repository }}:{{ .Values.global.initImage.tag }}"
          # busybox runs as root — override pod-level runAsNonRoot for init containers
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
          command:
            - sh
            - -c
            - |
              until nc -z {{ include "k8s-batch.mysql.host" . }} {{ .Values.mysql.service.port }}; do
                echo "Waiting for MySQL..."
                sleep 3
              done
        {{- if and .Values.kafka.enabled .Values.features.kafka }}
        # Uses headless service DNS (kafka-0.kafka-headless) instead of ClusterIP because the
        # ClusterIP service may not be ready before at least one Kafka pod passes readiness
        - name: wait-for-kafka
          image: "{{ .Values.global.initImage.repository }}:{{ .Values.global.initImage.tag }}"
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
          command:
            - sh
            - -c
            - |
              KAFKA_HOST="{{ include "k8s-batch.fullname" . }}-kafka-0.{{ include "k8s-batch.fullname" . }}-kafka-headless"
              until nc -z $KAFKA_HOST {{ .Values.kafka.service.port }}; do
                echo "Waiting for Kafka..."
                sleep 3
              done
        {{- end }}
        {{- if and .Values.kafka.enabled .Values.features.kafka .Values.schemaRegistry.enabled .Values.features.schemaRegistry }}
        - name: wait-for-schema-registry
          image: "{{ .Values.global.initImage.repository }}:{{ .Values.global.initImage.tag }}"
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
          command:
            - sh
            - -c
            - |
              until nc -z {{ include "k8s-batch.fullname" . }}-schema-registry {{ .Values.schemaRegistry.service.port }}; do
                echo "Waiting for Schema Registry..."
                sleep 3
              done
        {{- end }}
      containers:
        - name: app
          image: "{{ .Values.app.image.repository }}:{{ .Values.app.image.tag }}"
          imagePullPolicy: {{ .Values.app.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.app.securityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.app.config.serverPort }}
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: {{ .Values.app.springProfile | quote }}
            - name: SPRING_DATASOURCE_URL
              value: {{ include "k8s-batch.mysql.jdbcUrl" . | quote }}
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "k8s-batch.mysql.secretName" . }}
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "k8s-batch.mysql.secretName" . }}
                  key: password
            - name: SPRING_DATASOURCE_DRIVER_CLASS_NAME
              value: {{ include "k8s-batch.datasource.driverClassName" . | quote }}
            - name: SPRING_BATCH_JDBC_INITIALIZE_SCHEMA
              value: {{ .Values.app.config.batchInitializeSchema | quote }}
            {{- if and .Values.kafka.enabled .Values.features.kafka }}
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: {{ include "k8s-batch.kafka.bootstrapServers" . | quote }}
            {{- end }}
            {{- if and .Values.kafka.enabled .Values.features.kafka .Values.schemaRegistry.enabled .Values.features.schemaRegistry }}
            - name: SPRING_KAFKA_PROPERTIES_SCHEMA_REGISTRY_URL
              value: {{ include "k8s-batch.schemaRegistry.url" . | quote }}
            {{- end }}
            {{- if and .Values.kafka.enabled .Values.features.kafka }}
            - name: BATCH_TRANSACTION_INPUT_TOPIC
              value: {{ .Values.kafka.topics.transactionEvents.name | quote }}
            - name: BATCH_TRANSACTION_OUTPUT_TOPIC
              value: {{ .Values.kafka.topics.enrichedTransactionEvents.name | quote }}
            {{- end }}
            - name: SERVER_PORT
              value: {{ .Values.app.config.serverPort | quote }}
            {{- with .Values.app.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "k8s-batch.fullname" . }}-config
          livenessProbe:
            {{- toYaml .Values.app.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.app.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.app.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            {{- with .Values.app.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
        {{- with .Values.app.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
