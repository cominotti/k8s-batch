{{- if and .Values.kafka.enabled .Values.features.kafka }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "k8s-batch.fullname" . }}-kafka-topics
  labels:
    {{- include "k8s-batch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 5
  template:
    spec:
      restartPolicy: OnFailure
      initContainers:
        - name: wait-for-kafka
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z {{ include "k8s-batch.fullname" . }}-kafka {{ .Values.kafka.service.port }}; do
                echo "Waiting for Kafka broker..."
                sleep 5
              done
      containers:
        - name: create-topics
          image: "{{ .Values.kafka.image.repository }}:{{ .Values.kafka.image.tag }}"
          command:
            - bash
            - -c
            - |
              BOOTSTRAP="{{ include "k8s-batch.fullname" . }}-kafka:{{ .Values.kafka.service.port }}"

              kafka-topics --bootstrap-server $BOOTSTRAP \
                --create --if-not-exists \
                --topic {{ .Values.kafka.topics.partitionRequests.name }} \
                --partitions {{ .Values.kafka.topics.partitionRequests.partitions }} \
                --replication-factor {{ .Values.kafka.topics.partitionRequests.replicationFactor }}

              kafka-topics --bootstrap-server $BOOTSTRAP \
                --create --if-not-exists \
                --topic {{ .Values.kafka.topics.partitionReplies.name }} \
                --partitions {{ .Values.kafka.topics.partitionReplies.partitions }} \
                --replication-factor {{ .Values.kafka.topics.partitionReplies.replicationFactor }}

              {{- range .Values.kafka.topics.extra }}
              kafka-topics --bootstrap-server $BOOTSTRAP \
                --create --if-not-exists \
                --topic {{ .name }} \
                --partitions {{ .partitions }} \
                --replication-factor {{ .replicationFactor }}
              {{- end }}

              echo "All topics created successfully."
{{- end }}
