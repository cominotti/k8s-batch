{{- if and .Values.mysql.enabled (ne .Values.database.type "oracle") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8s-batch.fullname" . }}-mysql-init
  labels:
    {{- include "k8s-batch.labels" . | nindent 4 }}
    app.kubernetes.io/component: mysql
data:
  {{- if .Values.mysql.auth.additionalDatabases }}
  00-create-databases.sql: |
    -- Database-per-service: create additional logical databases beyond MYSQL_DATABASE ({{ .Values.mysql.auth.database }}).
    -- This script runs alphabetically before 01-spring-batch-schema.sql during MySQL first-start init.
    -- IMPORTANT: docker-entrypoint-initdb.d scripts only execute on first init (empty data dir).
    -- For existing PVCs, run these statements manually.
    {{- range $key, $dbName := .Values.mysql.auth.additionalDatabases }}
    CREATE DATABASE IF NOT EXISTS {{ $dbName }}
      CHARACTER SET {{ $.Values.mysql.config.mysqlCharacterSetServer }}
      COLLATE {{ $.Values.mysql.config.mysqlCollationServer }};
    GRANT ALL PRIVILEGES ON {{ $dbName }}.* TO '{{ $.Values.mysql.auth.username }}'@'%';
    {{- end }}
    FLUSH PRIVILEGES;
  {{- end }}
  01-spring-batch-schema.sql: |
    -- Spring Batch Core Schema for MySQL
    CREATE TABLE IF NOT EXISTS BATCH_JOB_INSTANCE (
      JOB_INSTANCE_ID BIGINT NOT NULL PRIMARY KEY,
      VERSION BIGINT,
      JOB_NAME VARCHAR(100) NOT NULL,
      JOB_KEY VARCHAR(32) NOT NULL,
      CONSTRAINT JOB_INST_UN UNIQUE (JOB_NAME, JOB_KEY)
    ) ENGINE=InnoDB;

    CREATE TABLE IF NOT EXISTS BATCH_JOB_EXECUTION (
      JOB_EXECUTION_ID BIGINT NOT NULL PRIMARY KEY,
      VERSION BIGINT,
      JOB_INSTANCE_ID BIGINT NOT NULL,
      CREATE_TIME DATETIME(6) NOT NULL,
      START_TIME DATETIME(6) DEFAULT NULL,
      END_TIME DATETIME(6) DEFAULT NULL,
      STATUS VARCHAR(10),
      EXIT_CODE VARCHAR(2500),
      EXIT_MESSAGE VARCHAR(2500),
      LAST_UPDATED DATETIME(6),
      CONSTRAINT JOB_INST_EXEC_FK FOREIGN KEY (JOB_INSTANCE_ID)
        REFERENCES BATCH_JOB_INSTANCE(JOB_INSTANCE_ID)
    ) ENGINE=InnoDB;

    CREATE TABLE IF NOT EXISTS BATCH_JOB_EXECUTION_PARAMS (
      JOB_EXECUTION_ID BIGINT NOT NULL,
      PARAMETER_NAME VARCHAR(100) NOT NULL,
      PARAMETER_TYPE VARCHAR(100) NOT NULL,
      PARAMETER_VALUE VARCHAR(2500),
      IDENTIFYING CHAR(1) NOT NULL,
      CONSTRAINT JOB_EXEC_PARAMS_FK FOREIGN KEY (JOB_EXECUTION_ID)
        REFERENCES BATCH_JOB_EXECUTION(JOB_EXECUTION_ID)
    ) ENGINE=InnoDB;

    CREATE TABLE IF NOT EXISTS BATCH_STEP_EXECUTION (
      STEP_EXECUTION_ID BIGINT NOT NULL PRIMARY KEY,
      VERSION BIGINT NOT NULL,
      STEP_NAME VARCHAR(100) NOT NULL,
      JOB_EXECUTION_ID BIGINT NOT NULL,
      CREATE_TIME DATETIME(6) NOT NULL,
      START_TIME DATETIME(6) DEFAULT NULL,
      END_TIME DATETIME(6) DEFAULT NULL,
      STATUS VARCHAR(10),
      COMMIT_COUNT BIGINT,
      READ_COUNT BIGINT,
      FILTER_COUNT BIGINT,
      WRITE_COUNT BIGINT,
      READ_SKIP_COUNT BIGINT,
      WRITE_SKIP_COUNT BIGINT,
      PROCESS_SKIP_COUNT BIGINT,
      ROLLBACK_COUNT BIGINT,
      EXIT_CODE VARCHAR(2500),
      EXIT_MESSAGE VARCHAR(2500),
      LAST_UPDATED DATETIME(6),
      CONSTRAINT JOB_EXEC_STEP_FK FOREIGN KEY (JOB_EXECUTION_ID)
        REFERENCES BATCH_JOB_EXECUTION(JOB_EXECUTION_ID)
    ) ENGINE=InnoDB;

    CREATE TABLE IF NOT EXISTS BATCH_STEP_EXECUTION_CONTEXT (
      STEP_EXECUTION_ID BIGINT NOT NULL PRIMARY KEY,
      SHORT_CONTEXT VARCHAR(2500) NOT NULL,
      SERIALIZED_CONTEXT TEXT,
      CONSTRAINT STEP_EXEC_CTX_FK FOREIGN KEY (STEP_EXECUTION_ID)
        REFERENCES BATCH_STEP_EXECUTION(STEP_EXECUTION_ID)
    ) ENGINE=InnoDB;

    CREATE TABLE IF NOT EXISTS BATCH_JOB_EXECUTION_CONTEXT (
      JOB_EXECUTION_ID BIGINT NOT NULL PRIMARY KEY,
      SHORT_CONTEXT VARCHAR(2500) NOT NULL,
      SERIALIZED_CONTEXT TEXT,
      CONSTRAINT JOB_EXEC_CTX_FK FOREIGN KEY (JOB_EXECUTION_ID)
        REFERENCES BATCH_JOB_EXECUTION(JOB_EXECUTION_ID)
    ) ENGINE=InnoDB;

    CREATE TABLE IF NOT EXISTS BATCH_STEP_EXECUTION_SEQ (
      ID BIGINT NOT NULL,
      UNIQUE_KEY CHAR(1) NOT NULL,
      CONSTRAINT UNIQUE_KEY_UN UNIQUE (UNIQUE_KEY)
    ) ENGINE=InnoDB;
    INSERT INTO BATCH_STEP_EXECUTION_SEQ (ID, UNIQUE_KEY)
      SELECT 0, '0' FROM DUAL
      WHERE NOT EXISTS (SELECT * FROM BATCH_STEP_EXECUTION_SEQ);

    CREATE TABLE IF NOT EXISTS BATCH_JOB_EXECUTION_SEQ (
      ID BIGINT NOT NULL,
      UNIQUE_KEY CHAR(1) NOT NULL,
      CONSTRAINT UNIQUE_KEY_UN2 UNIQUE (UNIQUE_KEY)
    ) ENGINE=InnoDB;
    INSERT INTO BATCH_JOB_EXECUTION_SEQ (ID, UNIQUE_KEY)
      SELECT 0, '0' FROM DUAL
      WHERE NOT EXISTS (SELECT * FROM BATCH_JOB_EXECUTION_SEQ);

    CREATE TABLE IF NOT EXISTS BATCH_JOB_INSTANCE_SEQ (
      ID BIGINT NOT NULL,
      UNIQUE_KEY CHAR(1) NOT NULL,
      CONSTRAINT UNIQUE_KEY_UN3 UNIQUE (UNIQUE_KEY)
    ) ENGINE=InnoDB;
    INSERT INTO BATCH_JOB_INSTANCE_SEQ (ID, UNIQUE_KEY)
      SELECT 0, '0' FROM DUAL
      WHERE NOT EXISTS (SELECT * FROM BATCH_JOB_INSTANCE_SEQ);
{{- end }}
